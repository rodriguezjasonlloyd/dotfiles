local config = {}

config.initial_cols = 110
config.initial_rows = 36
config.window_decorations = "RESIZE"
config.window_padding = {
    left = 0,
    right = 0,
    top = 0,
    bottom = 0,
}

return config
